function set_whole_volume() {
	/*
	if(keyboard_check_released(vk_down))
	    {
	    snd_vol=snd_vol-0.1
	    }
    
	if(keyboard_check_released(vk_up))
	    {
	    snd_vol=snd_vol+0.1
	    }
	*/    
	clamp(snd_vol, 0, 1)






}

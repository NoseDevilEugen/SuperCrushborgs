//draw_self()


scr_boss_blink()

fire[0].visible=on_fire
fire[1].visible=on_fire
fire[2].visible=on_fire


//draw_rectangle_color(x, y, x+(10*image_xscale), bbox_top, c_red, c_red, c_red, c_red, true)
//draw_set_color(c_white)
//draw_text(x, bbox_bottom+8, string(y))

//draw_rectangle(x, y-16, x+(10*image_xscale), y-4, true)
//draw_rectangle(x+(10*sign(hsp)), y, x+(20*sign(hsp)), y-30, true)
//draw_pantheon()

/*
if(mp_grid_path(global.grid, my_path, x, y, pathfind_x, o_zero.y, 1))
    {
    draw_path(my_path, x, y, false)
    /*
    for (var i=0;i<path_get_number(my_path);i++)
        {
        draw_point(path_get_point_x(my_path,i),path_get_point_y(my_path,i));
        }
    draw_text(x, y-50, string(path_get_number(my_path)))
    draw_text(x, y-70, string(point_direction(x, y, my_path_x, my_path_y)))
    }
*/
//path_delete(path)




/* */
/*  */
